# Task: component-b

Fix `src/component-b/multiply.mjs`. `multiply(a, b)` should return the
product of its two arguments; right now it does not, and
`src/component-b/multiply.test.mjs` fails because of it.

Write scope: `src/component-b/` only. Do not touch `src/component-a/`.

Done means: `node --test 'src/component-b/**/*.test.mjs'` passes, and your
fix is committed in your task worktree.
